# Scratch

Active execution packets can live here when a task needs a local plan, issue loop, or durable working state.

Do not create scratch state just to represent chat focus. Keep live doctrine in `.agents/`, completed plans in `history/plans/`, and dated evidence in `history/`.
